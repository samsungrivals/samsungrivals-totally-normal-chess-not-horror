{
  "name": "chess-app",
  "version": "1.0.0",
  "private": true,
  "description": "Chess app with skins, ELO, matchmaking, shop, and AI opponents",
  "scripts": {
    "start": "serve -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
